<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="org.societies.cis.android" android:versionCode="1" android:versionName="0.0.1-SNAPSHOT">

  <application android:icon="@drawable/icon" android:label="@string/app_name">
    <activity android:name=".CisManagerActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <provider android:name=".SocialProvider" android:authorities="@string/provider_authority" android:exported="true" android:syncable="true" android:enabled="true" android:readPermission="@string/provider_read_permission" android:writePermission="@string/provider_write_permission"></provider>
  </application>

</manifest>

