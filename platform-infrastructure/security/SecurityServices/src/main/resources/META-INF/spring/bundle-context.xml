<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/osgi 
		http://www.springframework.org/schema/osgi/spring-osgi-1.2.xsd
		http://www.springframework.org/schema/aop
		http://www.springframework.org/schema/aop/spring-aop.xsd
		http://www.springframework.org/schema/beans 
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context
		http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/osgi-compendium
		http://www.springframework.org/schema/osgi-compendium/spring-osgi-compendium.xsd
		http://www.springframework.org/schema/tx
		http://www.springframework.org/schema/tx/spring-tx.xsd"
	xmlns:task="http://www.springframework.org/schema/task"
	xmlns:tx="http://www.springframework.org/schema/tx"
	>

	<!-- regular spring configuration file defining the beans for this
	bundle. We've kept the osgi definitions in a separate 
	configuration file so that this file can easily be used
	for integration testing outside of an OSGi environment -->

	<context:annotation-config />
	<!-- Required for Async processing -->
	<tx:annotation-driven />
 
	<!--context:annotation-config /-->

	<bean id="securServicesProperties"
		class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
		<property name="systemPropertiesModeName" value="SYSTEM_PROPERTIES_MODE_OVERRIDE" />
		<property name="ignoreResourceNotFound" value="true"></property>
		<property name="locations">
			<list>
				<value>classpath:META-INF/spring/org.societies.local.properties</value>
				<value>file:${CONFIG_DIR}/org.societies.platform.properties</value>
			</list>
		</property>
	</bean>

	<bean id="signatureMgr"  class="org.societies.security.digsig.main.SignatureMgr" depends-on="secureStorage, certStorage" init-method="init">
		<property name="certStorage" ref="certStorage"/>
	</bean>

	<bean id="certStorage" class="org.societies.security.storage.CertStorage" depends-on="securServicesProperties" init-method="init">
		<property name="certFile" value="${cert.file}" />
		<property name="certPassword" value="${cert.password}" />
	</bean>

	<bean id="secureStorage" class="org.societies.security.storage.SecureStorage" depends-on="certStorage" >
	</bean>

</beans>
